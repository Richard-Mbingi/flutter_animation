import 'package:flutter/material.dart';

class MethodTwo extends StatelessWidget {
  const MethodTwo({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('Method 2'),
      ),
    );
  }
}
